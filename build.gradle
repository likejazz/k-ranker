apply plugin: 'java'
apply plugin: 'maven'

sourceCompatibility = 1.6
version = '1.0-SNAPSHOT'

ext {
        encoding = "UTF-8"
}

group = 'net.daum.search'

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
